/**
 * Phase 16 Wave 3 — Refer-a-friend route.
 *
 * Mounted at `/c/[slug]/my/refer`. The patient sees their share code with a
 * Telegram share CTA and the running list of pending/applied rewards.
 */
import { ReferScreen } from "../_components/refer-screen";

export default function ReferPage() {
  return <ReferScreen />;
}
